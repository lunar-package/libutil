description = "Utilities for lune luau"
files = ["*"]
main = "./lib/init.luau"
name = "libutil"
owner = "lune-package"
repository = "https://github.com/lune-package/libutil.git"
version = "0.1.4"
keywords = ["lune", "luau", "table"]

[dependencies]

[dev_dependencies]
